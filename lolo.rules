# curl.rules

# Curl User-Agent detection
# Alerte tcp pour l'utilisation de la commande CURL venant de partout vers mon réseau $HOME_NET, ici le 192.168.0.0/16, sur les ports 80 et 443

alert tcp any any -> $HOME_NET [80,443] (msg:"CURL Proof rules : Pikachu Go!"; flow:established,to_server; http.user_agent; content:"curl"; classtype:targeted-activity-custom; sid:10000002; rev:1;)
